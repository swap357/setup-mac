# Mac Shell Setup

## Install
```bash
./install.sh
./apply.sh
```

## Key bindings
- `Ctrl+R` - Fuzzy history search
- `Up/Down` - History search matching current input
- `z <dir>` - Jump to frequent directory
- `Tab` - Completion menu
